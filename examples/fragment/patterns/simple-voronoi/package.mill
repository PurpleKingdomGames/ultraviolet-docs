package build.examples.fragment.patterns.`simple-voronoi`

import indigoplugin.*

object `package` extends build.ShaderModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Simple Voronoi")
