package build.examples.fragment.textures.`basic-texture-use`

import indigoplugin.*

object `package` extends build.ShaderModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Basic Texture Use")
