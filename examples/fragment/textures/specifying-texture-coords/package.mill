package build.examples.fragment.textures.`specifying-texture-coords`

import indigoplugin.*

object `package` extends build.ShaderModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Specifying Texture Coordinates")
