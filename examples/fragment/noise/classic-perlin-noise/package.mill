package build.examples.fragment.noise.`classic-perlin-noise`

import indigoplugin.*

object `package` extends build.ShaderModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Classic Perlin Noise")
